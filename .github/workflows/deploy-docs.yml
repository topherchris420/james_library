name: Deploy Documentation to GitHub Page

on:
  push:
    branches:
      - main
    paths:
      - 'docs/**'      
      - 'papers/**'
      - '*_SOUL.md'
      - '*.md'
      - 'mkdocs.yml'
  workflow_dispatch:   

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install mkdocs-material

      - name: Stage documentation files
        run: |
          # Create subdirectories
          mkdir -p docs/agents docs/papers docs/records docs/assets
          
          # Copy files
          cp README.md docs/index.md
          cp ARCHITECTURE.md docs/
          cp SECURITY.md docs/
          cp AGENTS.md docs/agents/overview.md
          cp *_SOUL.md docs/agents/
          cp -r papers/* docs/papers/
          cp RAIN_LAB_MEETING_LOG.md docs/records/
          cp PRODUCT_ROADMAP.md docs/records/
          
          # Copy images
          cp -r assets/* docs/assets/

      - name: Build static site
        run: mkdocs build

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './site' 

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
